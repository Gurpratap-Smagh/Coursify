# Environment Variables for Production Deployment

# Backend API URL (Railway deployment)
VITE_API_BASE_URL=https://web-production-254279.up.railway.app

# Note: In development, the app uses '/api' which is proxied to localhost:8000
# In production, it uses the VITE_API_BASE_URL above
